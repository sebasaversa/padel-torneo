import { defineConfig } from 'vite';

export default defineConfig({
  // GitHub Pages publica el proyecto dentro de /padel-torneo/.
  // Las rutas relativas también permiten probar la build desde un subdirectorio.
  base: './'
});
